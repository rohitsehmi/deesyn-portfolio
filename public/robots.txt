# Unlisted, not private. The URL is meant to be pasted into an application;
# it is not meant to be crawled.
#
# Two things on this site are fine sent directly to Revolut and not fine
# sitting in a search index: the Ro x Revolut lockup, which conventionally
# reads as a partnership that does not exist, and Revolut's own icon assets,
# taken verbatim from assets.revolut.com.
#
# robots.txt only asks. The enforcement is the X-Robots-Tag header in
# vercel.json, which also covers the CV PDF -- a meta tag cannot.
User-agent: *
Disallow: /